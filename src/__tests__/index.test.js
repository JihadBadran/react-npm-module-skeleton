import React from "react";
import NautyDragon from "../index";
import { shallow, render, mount } from "enzyme";
import Context from "../Context";
// TODO this is a simple example of jest testing please refer to https://jestjs.io/docs/en/getting-started to see the complete documentation
describe("My NPM Module for the Great React!", () => {
  it("the first test", () => {
    expect(1 + 2).toBe(3);
  });
});
