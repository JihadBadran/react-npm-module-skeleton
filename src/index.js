/* TODO this is the entry point of the component
 * when a user import eh component with:
 * import Component from 'custom-component'
 * then it will return this:
 */
export default (props => <div>Component With Props:{props}</div>);
